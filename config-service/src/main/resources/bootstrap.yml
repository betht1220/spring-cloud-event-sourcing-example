server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      label: mod
      server:
        git:
          uri: https://github.com/betht1220/spring-cloud-event-sourcing-config.git
